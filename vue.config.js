module.exports = {
    // options...
    devServer: {
        // proxy: "https://alirezaonline.xyz"
    },
    // publicPath:
    //   process.env.NODE_ENV === "development" ? "http://localhost:8080/" : "/",
};